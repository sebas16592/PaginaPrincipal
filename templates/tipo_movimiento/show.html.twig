{% extends 'base.html.twig' %}

{% block title %}TipoMovimiento{% endblock %}

{% block body %}
    <h1>TipoMovimiento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_movimiento.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipo_movimiento.descripcion }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ tipo_movimiento.createdAt ? tipo_movimiento.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ tipo_movimiento.updatedAt ? tipo_movimiento.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_movimiento_index') }}">back to list</a>

    <a href="{{ path('tipo_movimiento_edit', {'id': tipo_movimiento.id}) }}">edit</a>

    {{ include('tipo_movimiento/_delete_form.html.twig') }}
{% endblock %}
